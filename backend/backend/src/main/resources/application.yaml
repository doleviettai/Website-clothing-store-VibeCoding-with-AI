spring:
  application:
    name: clothing-store-backend
  config:
    import:
      - optional:classpath:application-secret.yaml

  datasource:
    url: jdbc:mysql://localhost:3306/clothing_store?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh&allowPublicKeyRetrieval=true
    username: root
    password: taido231105
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      # update: tự động tạo/cập nhật bảng khớp entity nếu chưa có
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: false
    repair-on-migrate: true

# ===================== JWT =====================
jwt:
  secret: "QF/hXdGbeSViDt3Zkmem2iandRdI8MzqGqw2V9xMx2g="
  access-token-expiry: 900000
  refresh-token-expiry: 604800000

# ===================== CLOUDINARY =====================
cloudinary:
  cloud-name: "${CLOUDINARY_CLOUD_NAME:duc6hmjgx}"
  api-key: "${CLOUDINARY_API_KEY:152112139729355}"
  api-secret: "${CLOUDINARY_API_SECRET:WAxEHN67BBJ_T_uhkYvz2Oh2gxE}"
  folder: "${CLOUDINARY_FOLDER:FashionShop2}"

# ===================== CORS =====================
cors:
  allowed-origins: "${CORS_ALLOWED_ORIGINS:http://localhost:5173}"

# ===================== ZALOPAY =====================
zalopay:
  app-id: ${ZALOPAY_APP_ID:2553}
  key1: "${ZALOPAY_KEY1:SdngBKC0YTaIhhvStvFrHasRwOxWZImw}"
  key2: "${ZALOPAY_KEY2:trL0aYVssSonhBriefoAfAz0n2WnAohq}"
  create-order-url: "${ZALOPAY_CREATE_ORDER_URL:https://sb-openapi.zalopay.vn/v2/create}"
  query-order-url: "${ZALOPAY_QUERY_ORDER_URL:https://sb-openapi.zalopay.vn/v2/query}"
  callback-url: "${ZALOPAY_CALLBACK_URL:http://localhost:8080/api/v1/payments/zalopay/callback}"
  redirect-url: "${ZALOPAY_REDIRECT_URL:http://localhost:5173/payment-result}"

# ===================== MOMO =====================
momo:
  partner-code: "${MOMO_PARTNER_CODE:MOMO}"
  access-key: "${MOMO_ACCESS_KEY:F8BBA84260B7E544}"
  secret-key: "${MOMO_SECRET_KEY:K951B6E68B129F1E801545881D85703F}"
  create-order-url: "${MOMO_CREATE_ORDER_URL:https://test-payment.momo.vn/v2/gateway/api/create}"
  query-order-url: "${MOMO_QUERY_ORDER_URL:https://test-payment.momo.vn/v2/gateway/api/query}"
  ipn-url: "${MOMO_IPN_URL:http://localhost:8080/api/v1/payments/momo/ipn}"
  redirect-url: "${MOMO_REDIRECT_URL:http://localhost:5173/payment-result}"

# ===================== VNPAY =====================
vnpay:
  tmn-code: "${VNPAY_TMN_CODE:CGXZ24M1}"
  hash-secret: "${VNPAY_HASH_SECRET:XTYUIOPQASDFGHJKL1234567890MNBVC}"
  pay-url: "${VNPAY_PAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}"
  return-url: "${VNPAY_RETURN_URL:http://localhost:5173/payment-result}"

server:
  port: 8080
